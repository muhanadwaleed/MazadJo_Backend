Django>=5.0,<6
djangorestframework>=3.15,<4
djangorestframework-simplejwt>=5.3,<6
django-environ>=0.11,<1
django-cors-headers>=4.3,<5
drf-spectacular>=0.27,<1
psycopg[binary]>=3.2,<4
# Celery: eager in-process when REDIS_URL unset (OTP, beat schedules via DB)
celery>=5.4,<6
django-celery-beat>=2.6,<3
# ASGI stack (HTTP + WebSocket routes; in-memory channel layer without Redis)
channels[daphne]>=4.0,<5
# Metrics route is registered but disabled unless ENABLE_PROMETHEUS_METRICS=True
prometheus-client>=0.20,<1
# Serve static files (admin CSS/JS) directly from the ASGI process — no nginx needed
whitenoise[brotli]>=6.7,<7

pytest>=8.3,<9
pytest-django>=4.9,<5
virtualenv>=20.10.0,<22
pre-commit>=4.0,<5
black>=25.1.0,<26
isort>=5.13.2,<6
